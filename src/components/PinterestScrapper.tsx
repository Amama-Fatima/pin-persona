import React, { useState } from "react";

const PinterestScrapper = () => {
  const [keyword, setKeyword] = useState("");
  const [imageLimit, setImageLimit] = useState(20);
  return <div>PinterestScrapper</div>;
};

export default PinterestScrapper;
